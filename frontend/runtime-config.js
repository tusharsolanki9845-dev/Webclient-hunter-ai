/*
 * WebClient Hunter AI runtime configuration.
 *
 * Deployers may replace only the apiBase value after publishing the static site.
 * Do not place API keys, Supabase credentials, or any other secrets in this file.
 */
window.WCHA_RUNTIME_CONFIG = Object.freeze({
  apiBase: 'https://webclient-hunter-ai.onrender.com',
});
